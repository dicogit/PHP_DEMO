version: "3"
services:
   web:
     image: "${1}"
     ports:
     - 8001:80
     depends_on:
     - mysql
   mysql:
     image: "${2}"
     volumes:
     - db_data:/var/lib/mysql
     #environment:
     #   MYSQL_ROOT_PASSWORD: password
     #   MYSQL_DATABASE: mydatabase
volumes:
   db_data: {}